services:
  - type: web
    name: roman-nepali-ai-api
    env: python
    plan: free
    buildCommand: pip install -e ".[server]"
    startCommand: uvicorn roman_nepali_ai.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
